define( ["marionette"], function (Marionette) {

    // set up the app instance
    var Senarium = new Marionette.Application();

    // configuration, setting up regions, etc ...

    // export the app from this module
    return Senarium;
});